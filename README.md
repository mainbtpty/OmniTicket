# OmniTicket
Pre-made ticket bots often break when the bot crashes or reboots. This code utilizes Persistent Views (timeout=None), which means if a server owner restarts the bot, the old buttons still work flawlessly. 
